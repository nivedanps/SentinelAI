import React from 'react';
import { Link } from 'react-router-dom';
import { IncidentStatusBadge } from './IncidentStatusBadge';
import type { Incident } from './types';
import { formatLabel } from './utils';

export const IncidentTable: React.FC<{ items: Incident[] }> = ({ items }) => <div className="overflow-hidden rounded-xl border border-slate-200 bg-white dark:border-slate-800 dark:bg-slate-900"><div className="overflow-x-auto"><table className="min-w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500 dark:bg-slate-800/60"><tr><th className="px-4 py-3">Incident</th><th className="px-4 py-3">Type</th><th className="px-4 py-3">Severity</th><th className="px-4 py-3">Status</th><th className="px-4 py-3">Reported</th><th className="px-4 py-3" /></tr></thead><tbody>{items.map((incident) => <tr key={incident.id} className="border-t border-slate-100 dark:border-slate-800"><td className="px-4 py-3"><p className="font-semibold">{incident.title}</p><p className="max-w-xs truncate text-xs text-slate-500">{incident.address || 'No address supplied'}</p></td><td className="px-4 py-3 capitalize">{formatLabel(incident.disaster_type)}</td><td className="px-4 py-3"><IncidentStatusBadge value={incident.severity} /></td><td className="px-4 py-3"><IncidentStatusBadge value={incident.current_status} /></td><td className="px-4 py-3 text-slate-500">{new Date(incident.created_at).toLocaleDateString()}</td><td className="px-4 py-3"><Link to={`/incidents/${incident.id}`} className="font-semibold text-blue-600 hover:underline">View</Link></td></tr>)}</tbody></table></div></div>;
